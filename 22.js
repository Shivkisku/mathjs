// Given values
const bananaValue = 15;
const cherryValue = 5;
const appleValue = 4;

// Calculate the total for 1 banana, 1 cherry, and 1 apple
const total = bananaValue + cherryValue + appleValue;

// Print the result
console.log("Total value for 1 banana + 1 cherry + 1 apple:", total);
