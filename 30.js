// Given values
const area = 16;   // in square centimeters
const diagonal1 = 4; // in centimeters

// Calculate the length of the other diagonal
const diagonal2 = (2 * area) / diagonal1;

// Output the result
console.log(`Length of the other diagonal: ${diagonal2} cm`);
