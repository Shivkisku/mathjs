// Given equation: (23x / 2) = 46

// Right-hand side of the equation
const rightHandSide = 46;

// Coefficient of x
const coefficientOfX = 23 / 2;

// Solve for x
const solution = (rightHandSide * 2) / 23;

// Output the result
console.log(`Solution for x: ${solution}`);
