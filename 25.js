// Given equation: x - 11 = 7

// Add 11 to both sides to isolate x
const constantTerm = 11;
const rightHandSide = 7;

const solution = rightHandSide + constantTerm;

// Output the result
console.log(`Solution for x: ${solution}`);
