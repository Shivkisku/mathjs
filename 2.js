// Find the zero of the polynomials in each of the following cases:

// (i) p(x) = x+5 

// Define the polynomial expression
const p = (x) => x + 5;

// Solve for x when p(x) = 0
const zero = -5;

// Print the zero
console.log("Zero of the polynomial:", zero);
