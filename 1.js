//Is zero a rational number? Can you write it in 
//the form p/q where p and q are integers and q ≠ 0?

let p = 0;
let q = 1;

// Zero in the form p/q
let zeroAsRational = p + "/" + q;

console.log(zeroAsRational); // Output: 0/1
